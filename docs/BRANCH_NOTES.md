# Branch notes

The package skeleton is ready for review. The branch keeps heavy model and media
dependencies outside the core package and establishes immutable domain records
for the next checkpoint.
