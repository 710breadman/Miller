# STO-003 — Global sequence optimizer


- **Parent roadmap item:** `R6`
- **Current planning status:** `blocked`
- **Exact objective:** Optimize all scene choices jointly for relevance, continuity, reuse, quality, pacing, era, and owner locks.
- **Why it matters:** It advances Miller’s evidence-based critical path without reopening proven foundations or widening unrelated scope.
- **Files allowed to inspect:** `src/miller/storyboard/**`, `src/miller/audio/models.py`, `src/miller/retrieval/**`, `src/miller/analysis/models.py`, `tests/test_storyboard.py`, and evaluation fixtures/manifests.
- **Files allowed to change:** card-specific `src/miller/storyboard/**`, matching models/tests, an ADR when optimization policy changes, and reports.
- **Files that must not be changed:** `src/miller/db.py`, migrations, `web/**`, renderer internals, and source media.
- **Required context:** `PROJECT_CHARTER.md`, the executive section of `CURRENT_STATE.md`, `SPRINT_STATE.json`, this card, `HANDOFF.md`, Git status/diff, and only directly relevant architecture/decision excerpts.
- **Prerequisites:** `STO-002`.
- **Inputs:** the approved repository evidence, fixture/manifests, labels, or local reports named by this objective; never fabricate results or commit private media.
- **Detailed task sequence:**
  1. Confirm prerequisites, active state, branch, and allowed path patterns; stop if they conflict.
  2. Inspect existing implementation and tests relevant to this objective: Optimize all scene choices jointly for relevance, continuity, reuse, quality, pacing, era, and owner locks.
  3. Write or update the smallest design, schema, fixture, or harness needed; record assumptions and rollback.
  4. Implement or execute only this card; preserve backward compatibility, source hashes, and existing fallbacks.
  5. Add/run focused tests, inspect generated artifacts, and run the full gate for production changes.
  6. Record evidence level, exact commands/results, failures, versions, and next action; update control files once after review.
- **Expected outputs:** A bounded implementation/design/report, focused tests, exact provenance, and an evidence record.
- **Tests to add or run:** focused unit/contract/integration tests for changed behavior; `uv run python scripts/validate_planning.py` for control changes; full Ruff, strict Mypy, Pytest, and build gate for production changes; E3/E4/E5/E6 gates when required.
- **Acceptance criteria:** objective completed inside allowed paths; focused/full gates pass as applicable; artifacts are inspected; source inputs are unchanged; evidence class and remaining proof are explicit; independent reviewer accepts the result.
- **Failure conditions:** prerequisite absent; unrelated paths are required; the same root failure occurs twice; a schema/public-interface/license conflict appears; source mutation is possible; or required evidence cannot be produced.
- **Recovery instructions:** stop new edits, preserve diff/logs/artifacts, remove or quarantine unsafe partial outputs, update `BLOCKERS.md` and `HANDOFF.md`, and activate a dependency-valid fallback card.
- **Logging requirements:** command, commit, environment, dependency/model versions, input/output hashes, elapsed time, RAM/VRAM where relevant, warnings/errors, and artifact/report paths.
- **Documentation updates:** `CURRENT_STATE.md` when accepted evidence changes; `DECISIONS.md`/ADR for choices; `RISKS.md`/`BLOCKERS.md` for new constraints; `HANDOFF.md` always; `SPRINT_STATE.json` only after review.
- **Git checkpoint:** one branch and one coherent commit ending with `STO-003` in the message; stage no unrelated files.
- **Estimated context load:** high.
- **Recommended model:** Gemma 4 12B for bounded implementation or data preparation; Codex owns design and final review.
- **Codex review requirement:** mandatory before evidence/state advancement; architecture, schema, license, and public-interface changes require Codex approval before implementation.
- **Next unblocked sprint options:** direct successors when all other prerequisites and external gates pass: `STO-004`; otherwise use the current fallback pool `INV-003`, `DOC-001`, `ARC-001`, `ARC-003`, `SEC-001`, `SEC-002`, `QAE-001` after checking `SPRINT_STATE.json`.
