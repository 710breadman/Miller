# Implemented now

The branch contains the executable package skeleton, immutable core schemas,
package metadata, and smoke tests. It is intentionally smaller than later
persistence and media checkpoints.
