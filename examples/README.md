# Examples

- `config.toml`: safe local defaults
- `scenes.json`: manual FFmpeg render request; replace all example paths
- `script_request.json`: minimal evidence-led Ollama request using a placeholder
  hash and source; replace with real approved evidence before use
